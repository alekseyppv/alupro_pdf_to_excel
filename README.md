# AluPro PDF → Excel Parser

Программа извлекает данные о конструкциях из PDF (AluPro / Alutech расчёты)
и формирует таблицу Excel в формате CSV-структуры.

Поддерживается drag & drop файла прямо в окно.

---

## 📌 Что извлекается из PDF

С каждой страницы:

| Поле       | Откуда берётся                  | Пример                 |
| ---------- | ------------------------------- | ---------------------- |
| Позиция    | `Позиция:`                      | ОК1                    |
| Описание   | `Описание:` (в нижнем регистре) | окно                   |
| Система    | `Система:` → `Alutech F50`      | AluminTechno → Alutech |
| (пусто)    | разделитель                     |                        |
| Цвет       | `Цвет:`                         | RAL7016М               |
| (пусто)    | разделитель                     |                        |
| Ширина     | `Рама:` до `x`                  | 1200                   |
| Высота     | `Рама:` после `x`               | 1400                   |
| Количество | `Кол-во:`                       | 2                      |

---

## 🖥 Интерфейс

* Выбор PDF через кнопку
* Drag & Drop PDF
* Выбор пути сохранения Excel
* Проверка формата
* Сообщение о завершении

---

## 📦 Требования

Python 3.10 – 3.14

Установка зависимостей:

```bash
pip install pymupdf pandas openpyxl tkinterdnd2
```

---

## ▶ Запуск

```bash
python ap.py
```

---

## 📁 Формат результата

Excel без заголовков:

```
ОК1,окно,Alutech F50,,RAL7016М,,1200,1400,2
```

---

## ⚙ Особенности

* Автоматически заменяет `AluminTechno` → `Alutech`
* Обрезает систему до модели (F50, W72 и т.д.)
* Числа сохраняются как числа
* Работает на Windows 11 (новый проводник)

---

## 🧱 Сборка в EXE

### 1. Установка PyInstaller

```bash
pip install pyinstaller
```

---

### 2. Сборка

В папке с `ap.py`:

```bash
pyinstaller --noconsole --onefile ap.py
```

После сборки файл будет:

```
dist/ap.exe
```

---

## ❗ Важно (TkinterDnD)

TkinterDnD требует DLL, поэтому собираем с добавлением данных:

```bash
pyinstaller --noconsole --onefile ^
--add-data "C:\Users\%USERNAME%\AppData\Local\Programs\Python\Python314\Lib\site-packages\tkinterdnd2;tkinterdnd2" ^
ap.py
```

---

## 🧪 Проверка

Запусти:

```
dist/ap.exe
```

Перетащи PDF → выбери путь → ОБРАБОТАТЬ

---

## 📜 Лицензия

MIT
